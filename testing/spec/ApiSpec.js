describe('Hello world', function() {

  it('says hello', function() {
    expect(helloWorld()).toEqual('Hello world!');
  });

  it('a is === 2', function() {
    var a = 2
    return a;
    expect(HelloWorld()).toEqual(2);
  });

});